[package]
name = "punks-browser"
version = "0.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
punks-core = { path = "../punks-core" }
punks-analysis = { path = "../punks-analysis" }
punks-library = { path = "../punks-library" }
punks-playback = { path = "../punks-playback" }
log = "0.4"
