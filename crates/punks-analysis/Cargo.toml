[package]
name = "punks-analysis"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

# Pure time-domain DSP: no dependencies (std only). Keep it that way until a
# second real need justifies shared infra.
[dependencies]
