# Issue tracker

This repository tracks work in GitHub Issues for `samanshaiza004/punks`.

Use the `gh` CLI for issue operations. Pull requests are not treated as a separate request surface by this configuration.
